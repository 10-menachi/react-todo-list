const Title = () => (
  <header style={{
    padding: '20px 0',
    lineHeight: '1.5em',
    color: '#aeadad',
    textAlign: 'center',
  }}
  >
    <h1 style={{ marginBottom: '1rem' }}>Todos</h1>
    <h4>Items will persist in the browser local storage</h4>
  </header>
);

export default Title;
